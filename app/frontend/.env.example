# RiceGuard Frontend Environment Variables Example
# In development (when using Vite dev proxy), leave empty or set to /api
# In production, set to your deployed backend API URL (e.g., on Render, Railway, Fly.io)

VITE_API_BASE_URL=https://riceguard-api.onrender.com/api
