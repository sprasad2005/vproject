# Dataset Inventory Reports

This directory stores automatically generated dataset inventory reports, structural audits, and integrity scans produced during Phase 0.

## Artifacts
* `dataset_inventory.json`: Machine-readable metadata describing detected datasets, class distributions, file counts, and integrity flags.
* `dataset_inventory.md`: Human-readable summary table documenting discovered dataset paths, role permissions, bounding-box annotations, and image-mask pairing status.
